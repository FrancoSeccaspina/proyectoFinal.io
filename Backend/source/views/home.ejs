<!DOCTYPE html>
<html lang="es"> 
<head>
  <%- include('./partials/head.ejs') %>
  <!--
  <audio id="background-music" autoplay loop>
    <source src="/music/soundtrack.mp3" type="audio/mpeg">
  </audio>
  -->
</head>
<body>
  <%- include('./partials/header.ejs') %>

  <main class="main-content">
    <div class="carousel-container" role="region" aria-label="Galería de imágenes destacadas">
      <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
        
        <div class="carousel-indicators">
          <% for(let i = 0; i < 8; i++) { %>
            <button 
              type="button" 
              data-bs-target="#carouselExampleIndicators" 
              data-bs-slide-to="<%= i %>" 
              class="<%= i === 0 ? 'active' : '' %>" 
              aria-label="Diapositiva <%= i + 1 %>"
              aria-current="<%= i === 0 ? 'true' : 'false' %>">
            </button>
          <% } %>
        </div>

     <div class="carousel-inner">
     <% for(let i = 1; i <= 8; i++) { %>
     <div class="carousel-item <%= i === 1 ? 'active' : '' %>">
     <img 
     src="/images/carrouselve<%= i %>_suave.png" 
     class="d-block w-100 carousel-img" 
     alt="Imagen ilustrativa número <%= i %> del gimnasio ActivaFitness">
       </div>
        <% } %>
      </div>



        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Anterior</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Siguiente</span>
        </button>
      </div>
    </div>
    <!--
    <div class="planes" role="region" aria-label="Planes disponibles">
      <section class="planMensual">
        <img src="/images/planMensual.png" alt="Imagen descriptiva del plan mensual">
        <a href="https://api.whatsapp.com/send?phone=5491166601356" aria-label="Consultar sobre el plan mensual vía WhatsApp">Consulta a tu asesor</a>
      </section>
      <section class="planAnual">
        <img src="/images/planAnual.png" alt="Imagen descriptiva del plan anual">
        <a href="https://api.whatsapp.com/send?phone=5491166601356" aria-label="Consultar sobre el plan anual vía WhatsApp">Consulta a tu asesor</a>
      </section>
    </div>
 -->
  </main>
 
  <%- include('./partials/footer.ejs') %>
  <!--
  <div class="gestion-boton">
    <button onclick="window.location.href='/admin/index.html'">Ir al sistema de gestión</button>
  </div>
  -->

</body>
<script src="./js/hamburguer.js"></script>
<script src="./js/soundtrack.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</html>