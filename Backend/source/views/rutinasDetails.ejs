<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de Rutinas</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Ruta a tu archivo CSS -->
</head>
<body>
    <header>
        <h1>Detalles de Rutinas</h1>
    </header>
    <main>
        <% if (success) { %>
            <h2>Ejercicios para el grupo muscular <%= grupoMuscularId %></h2>
            <ul>
                <% ejercicios.forEach(ejercicio => { %>
                    <li>
                        <strong>Nombre:</strong> <%= ejercicio.nombre %><br>
                        <strong>Descripción:</strong> <%= ejercicio.descripcion %>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <div class="error">
                <p><%= errors.msg %></p>
            </div>
        <% } %>
    </main>
    <footer>
        <p>&copy; 2025 - Tu aplicación de rutinas</p>
    </footer>
</body>
</html>