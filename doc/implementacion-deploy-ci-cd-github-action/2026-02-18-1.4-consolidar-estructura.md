# [1.4] Consolidar estructura de carpetas del repo único

**Fecha:** 2026-02-18

## Resumen

Verificación de que el proyecto opera como repositorio único (monorepo) y actualización del `README.md` para reflejar la estructura real, los comandos correctos y la ausencia de dependencia con un segundo repositorio de build.

## Verificación de segundo repositorio

- **No existe** un segundo repositorio de build sincronizado.
- El repositorio tiene un único remote: `git@github.com:FrancoSeccaspina/proyectoFinal.io.git`
- No existe archivo `.gitmodules` → no hay submódulos.
- **Conclusión:** el proyecto ya opera como repositorio único. No se requieren acciones de migración.

## Cambios Principales

- Se reescribió `README.md` para:
  - Declarar explícitamente que es el repositorio único del proyecto
  - Documentar la estructura de carpetas real (`Backend/`, `frontend/`, `flask/`, `nginx/`, `scripts/`, `doc/`)
  - Incluir diagrama de arquitectura (Nginx → Express / Flask → MySQL)
  - Corregir la referencia al script de compilación: `build_frontend.sh` → `scripts/compilar_frontend.sh`
  - Agregar instrucciones para configurar variables de entorno a partir de `.env.example`
  - Separar claramente comandos de desarrollo por componente

## Archivos Afectados

| Archivo | Cambio |
|---------|--------|
| `README.md` | Reescrito para reflejar estructura actual y repositorio único |
| `doc/2026-02-18-1.4-consolidar-estructura.md` | Documento de registro de este ticket (este archivo) |

## Criterios de Aceptación

- [x] Confirmado que no hay dependencia con un segundo repositorio
- [x] Documentación (`README.md`) actualizada para reflejar el repositorio único
